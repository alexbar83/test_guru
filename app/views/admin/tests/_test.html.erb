
<tr>
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>

  <td><%= link_to 'show', admin_test_path(test) %></td>
  <td><%= link_to 'edit', edit_admin_test_path(test) %></td>
  <td><%= link_to 'delete',
                    admin_test_path(test), data: {
                   	turbo_method: :delete,
                   	turbo_confirm: "Are you sure? "} %></td>
</tr>
